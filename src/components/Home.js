import React from 'react';
import styled from 'styled-components';

function Home() {
  return <HomeContainer>This is the home</HomeContainer>;
}

export default Home;

const HomeContainer = styled.div``;
